//this is the test file
#include <stdio.h>
void test_add_task() {
  // Setup tasks, call addTask, then assert expected results
  //assert(addTask(...) == expected_value);
  // More asserts...
}

int main() {
  test_add_task();
  printf("All tests passed!\n");
  return 0;
}